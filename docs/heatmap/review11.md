# review10：v3 spec 第二轮复审（spec-reviewer，2026-08-15）

审查对象：`docs/heatmap/spec.md` v3（一轮修订后全文重写版）
审查方式：逐条核对 B1/B2/S1-S6/O1-O5 落实 + 全文数字/接口实测复核
（roster.json、goals/hoops_batch*.json、crop_scorers.py 源码）
结论：**通过（无阻断）**——建议级 1 条（开工前一行补丁），可选 3 条

## 整体评价

一轮 2 阻断 + 6 建议 + 5 可选全部真实落地，无纸面整改。新增的两个侦察事实
（players.team 字段、锚点 cx 双峰）经本轮独立复算属实。B1 归一化数学正确
（相对坐标以各球自身筐心为原点，大端 (dx,dy) 取反即绕筐心旋转 180°，不要求
两端筐心像素重合）；B2 队别链路与 roster 实际 schema 完全吻合。唯一实质问题
是侦察数字"hoops 覆盖 122/122"按 spec 自述口径复算为 121/122，且坐标口径
缺零覆盖分支——实测恰有 1 球触发该分支，一行补丁可修，不阻断架构。

## 一轮问题逐条核验（全部落实）

### 阻断

- **B1 筐端归一化 ✓**：坐标口径节写死 cx 中位切分两端、统一归一到 cx 均值
  较小端、大端落点绕筐心旋转 180° 取反、`flipped: bool` 逐球落盘
  （goal_landings.json 字段）并进报告（"两端锚点分布与翻转端标记"）。
  audit 拼图映射层标注齐：文字标注含 event_key / rel_xy_m / flipped /
  映射后模板落点。不做归一的污染机理（镜像到篮板后）也写明了。
- **B2 队别口径 ✓**：独立成节。本轮实测复核 roster.json：players 每项含
  独立 `team` 字段（如 `黑后卫→车百鼎`），assignments 106 条 value=tag
  全量查得到（0 缺失），team 分布 车百鼎 53 / 半截篮 51 / 便服 2——与
  spec 数字一致。便服不进热图 + 逐球 WARNING、tag 查不到记 uncovered、
  队名渲染用 roster 原值，三条全部写死。

### 建议

- **S1 ✓**：串人守卫改引 `team_of_box(整帧+框)`——源码核实
  crop_scorers.py:1118 签名 `team_of_box(img: Image.Image, box: Box) -> str`，
  返回值 "黑"/"白"/"便服"；"便服不触发剔除"与 drop_opposite_team
  （:1140，"便服"帧一律保留、种子便服不过滤）同口径。依赖段已改
  "帧图用途：串人守卫颜色判定（team_of_box）+ 目击拼图"，帧图进主路径
  依赖声明到位。
- **S2 ✓**：结构性上限声明写入落点口径；源码核实 TRACE_WINDOW_SEC=2.0
  （crop_scorers.py:105），"种子帧早于 anchor−3.0s 目标帧越窗"算术正确
  （anchor−1.0s 目标 − 种子 ±2.0s 窗），"59% 已含天花板、不靠扩窗提覆盖"
  与侦察口径自洽。
- **S3 ✓**：前提声明入"目标"节；实测 roster.confirmed=false 属实；"归属
  经人工认过但未最终导出确认、目击只验落点不验归属"表述准确。
- **S4 ✓（见 J1 残留）**：多事件取舍写死（采样时刻距锚点最近→空间距离
  最近）；但零覆盖分支未定义，见建议级 J1。
- **S5 ✓**：覆盖选择性偏差列为 calib-report 强制项（"主路偏好静止出手，
  突破/强对抗易断链"），边界节重申"不宣称不歪分布"。
- **S6 ✓（见 K2 残留）**：广告静物风险引经验教训 §3，判据①"人框是真人"
  覆盖目击侧；报告清单未显式列此项，见可选 K2。

### 可选

- **O1 ✓**：106/122、63/106=59%、28/106=26%、未标记 16 球均带分母。
- **O2 ✓**：输入改 glob `goals_batch*.json` 等，不硬编码批次号。
- **O3 ✓**：目击判据量化（①真人/投篮者对照锚点帧；②映射后界内且距筐
  ≤10m）；audit 拼图含锚点帧缩略并列。
- **O4 ✓（见 K2 残留）**：兜底路"最近人框可能是防守人、无同色校验"噪声
  声明在落点口径写明；报告清单未显式列，见 K2。
- **O5 ✓**：贴边底边中点+框高双失真写入坐标口径，报告误差声明含"贴边
  失真"。

## 数字复核记录（本轮独立复算）

- goals 总数 122（29+49+44）✓；assignments 106、team 0 缺失、53/51/2 ✓
- 28/106=26.4%≈26% ✓；63/106=59.4%≈59% ✓；122−106=16 ✓
- hoops 锚点 cx（全量 detected 287 个）：P25=563.0 / P75=1264.5 ≈ spec 的
  560/1265 ✓（口径为全量锚点，建议括注；双峰结论与中位切分逻辑不受影响）
- **hoops 覆盖 122/122 ✗→121/122**：按 spec 自述口径（detected 且 window
  含锚点）复算，`dji_mimo_20260805_190546_0035` 锚点 1.2s，该文件唯一
  detected 事件 window=[1.5,6.4]，锚点在窗外。详见 J1。

## 分级问题清单

### 建议级（1 条，开工前落实，一行补丁量级）

- **J1 侦察数字失真 + 坐标口径零覆盖分支未定义**：spec 称"hoops 筐轨迹
  覆盖 122/122（detected 且 window 含锚点）"，按此字面口径实测 121/122
  （190546_0035，锚点 1.2s 早于唯一 detected 事件窗起点 1.5s）。连带
  缺口：坐标口径主句"原点=锚点时刻筐心（track 按采样时刻最近取值）"
  未限定 window 覆盖，S4 补充句只管"多事件同时覆盖"的取舍，**零事件
  覆盖锚点时取哪个 track 无定义**——而真实数据恰有 1 球触发。修订建议：
  数字改为 121/122 并括注例外球；坐标口径补一句"无 window 覆盖锚点时
  取采样时刻距锚点最近的 detected 事件，记 WARNING"。

### 可选（3 条）

- **K1 覆盖率分母口径**：成功标准"106 球中覆盖 ≥55%"未写明便服 2 球
  （不进热图）是否计入分母；建议明示（如"分母 106 含便服，便服球不计
  覆盖"或分母改 104）。
- **K2 报告声明项清单不全**：O4 兜底路噪声、S6 广告静物在正文均有"报告
  声明"承诺，但 calib-report 交付物枚举（误差声明/选择性偏差/便服数量/
  roster 风险）未列这两项，验收时易漏写；建议清单补全。
- **K3 队名白名单写死**：队别口径"本场两队（车百鼎/半截篮）"硬编码队名，
  与 AGENTS.md"素材流动、不硬编码清单"精神轻微张力；可改动态口径
  （players team 值集合 ∖ {便服}）。场次级 spec 写死可接受，故列可选。

## AGENTS.md 冲突对照表

| 条目 | spec 表现 | 结论 |
|---|---|---|
| 中间产物放 work/、成品放 output/<场次>/ | goal_landings.json/audit 拼图落 work，热图 PNG 落 output/<场次>/ | 无冲突 |
| 命名规范（队伍_XX_*） | `队伍_XX_进球热图.png` 沿用前缀风格（新产物类型，规范未覆盖 PNG） | 无冲突 |
| 素材流动、不硬编码文件清单/数量 | 批次输入改 glob（O2）；队名写死见 K3（轻微） | 无阻断冲突 |
| roster 按场次隔离、各自需用户确认 | S3 披露 confirmed=false 风险，"接受此风险（立哥决策）"随 spec 批准生效 | 无冲突（决策待批准即成立） |
| 不改 scripts/ 现有对外行为 | 边界节明确；S2 明确"不改 crop_scorers 对外行为" | 无冲突 |
| 四件套先行、文档自审 | spec v3 先行，本轮即第二轮 spec-reviewer 审查 | 合规 |
| 经验教训 §8"若三启热图"条 | v3 路线与该条逐字吻合（绕开球检测、roster 驱动 trace_person、锚点前脚位）；"前提是认人完成"与 S3 风险披露并存不矛盾 | 兼容 |

## 结论

**通过（无阻断、无 AGENTS.md 冲突）**。J1 建议开工前顺手落实（数字 121/122
+ 零覆盖分支一句话）；K1-K3 可选，不挡 plan/todo 阶段。
