# spec：进球热区图 · 阶段 0 标定验证

日期：2026-08-15 · 前置：`docs/heatmap/research.md`（预研档，路线与风险依据）
提出：立哥 · 状态：review01/02 两轮审查通过、立哥批准动工；2026-08-15 阶段 0
收尾——Q2 实测 63.1% < 70%（todo.md 状态段 + 经验教训 §8），功能证伪归档

## 目标

不动热图成品，先用最小成本回答两个 go/no-go 问题（research.md §4 判据写死）：

- **Q1 配准精度**：人工点关键点的单应变换，在车百鼎素材上反投影误差中位
  是否 ≤ 0.5 米？
- **Q2 回溯成功率**：122 球（`work/20260805_车百鼎/goals_batch*.json`
  confirmed 29+49+44=122）中，≥ 70% 能否回溯到稳定出手点？

**两条都过关 → 立项阶段 1；任一不过关 → 功能证伪，结论记经验教训，不纠缠。**

## 出手点口径（一轮审查 B2 修订，核心定义）

**热图落点 = 出手人脚部位置，不是球心像素。**理由：地面单应只映射地平面，
出手瞬间球离地约 2m，球心反投影得到的是"相机→球视线与地面的交点"，近景
低机位下系统性偏差米级起步——球心口径下 Q1/Q2 双双过关也证明不了热图可用。

- 球位（mot_cache `balls`）只用于定位**出手时刻**与**持球人**
- 落点取持球人 **persons 框底边中点**（同一缓存内，无新检测；
  crop_scorers 轨迹法找持球人是已验证先例）

## 交付物

1. `scripts/release_probe.py`（回溯实测器，纯只读）：
   对 122 球逐球处理——锚点前 0.4~2.5s 窗口（约 11 帧）内取球位定出手时刻
   与持球人 → 人脚底边中点落点；输出覆盖率/断帧分布/一致性报告
   （`work/20260805_车百鼎/release_probe.json` + 控制台汇总）。
   **不改 mot_cache、不改任何现有产物**
   - 窗口从 research.md 的"锚点前 1~2s"细化为 0.4~2.5s：排除入网飞行段
     （0.2s 跳 800px 的瞬移发生在该段，经验教训 §3）、扩大持球段搜索面
   - "稳定"定义写死：有球帧按间隔 ≤2 帧（0.4s）分段，段内 ≥2 个有球帧且
     首尾位移 ≤60px 记一段稳定段；断帧不重置但只有球帧参与位移计算
   - 输入契约（rules.md §0.2）：goals 读入先过 schema 校验；`file` 去 `.mp4`
     得 fid；缺帧目录/缺缓存的球记 WARNING 跳过且**计入分母**（保守口径）
2. `scripts/gen_calib_page.py`（配准标注小页生成器，HTML 单页，基建仿
   gen_label_page.py：自包含、localStorage、导出 JSON）：
   抽样 ≥10 个事件——分层维度：左右端 × 远近 × 不同片段 × 镜头朝向/变焦档
   （research.md §0 实测同筐不同片段像素差 ~300px，变焦是配准误差主变量）。
   每事件展示出手帧，立哥点 **≥5 个**场地线交点（禁区四角 + 罚球线端点/
   三分弧与边线交点等，**沿纵深铺开**，勿全挤筐周——单应 8 自由度最少 4 点
   可解，留一法评估需 n−1 ≥ 4，且点位集中会让远处条件数恶化），
   导出 `calib.json`（落 `work/20260805_车百鼎/`；schema：event_key / fid /
   frame / points[{px,py, landmark}] / court_spec 版本号）
3. `scripts/calib_eval.py`（精度评估）：
   calib.json → cv2.findHomography（留一法逐点反投影误差）→ 米制换算
   （FIBA 已知尺寸锚定：三分 6.75m、禁区 4.9×5.8m）→ 中位/P90 报告
   （`work/20260805_车百鼎/calib_eval.json`）
4. 标定报告：`docs/heatmap/calib-report.md`（独立命名；reviewNN 序列专属
   spec-reviewer 审查报告，不混用），含双门槛结论（过/不过 + 实测数字）

## 边界（不做）

- 不画热图成品、不动 matplotlib 模板（阶段 1 的事）
- 不改 `scripts/` 现有任何文件的对外行为；三个新脚本全部只读现有产物
- 不做自动关键点检测模型（阶段 3 候选，本期不碰）
- 抽样事件的人工点关键点由立哥完成（≥10 事件 × ≥5 点；先 1 事件试手
  估时，再排总量）

## 成功标准（= go/no-go 判据，预先写死，防现场改目标）

- Q1：全部留一误差点**混合取中位 ≤ 0.5 米**（P90 同时报告供参考；
  样本 = 抽样事件 × 逐点留一）
- Q2：122 球中 **≥ 70%** 回溯到稳定出手点（定义见交付物 1）；
  一致性抽查 10 球：±0.4s 窗口平移两次回溯落点像素距离 ≤ 100px
- 质量门：ruff format/check 干净、pytest 全绿（新脚本各带单测：
  回溯启发式的合成轨迹用例、单应误差计算的已知坐标用例）

## 依赖与环境

- cv2（opencv 5.0.0 已装）findHomography + perspectiveTransform
- 页面基建仿 gen_label_page.py（自包含 HTML、localStorage、导出 JSON）
- 抽样事件帧图从 `work/frames/<fid>/f_*.jpg` 现取（已在盘上，1920×1080；
  车百鼎 148 文件经 session_facts 核实全部 3840×2160/59.94fps，无 4:3，
  本期无多尺寸口径问题；他场次复用时再按 session_facts 注入）

## 开放问题

1. 抽样名单由我按分层维度选出，立哥只负责点——若立哥想自己圈事件，
   calib 页支持换事件（←/→ 翻页）
2. 尺度锚定默认 FIBA 标准尺寸；车百鼎场馆若为非标场地，误差判据等比
   放宽——默认按 FIBA，报告里注明
