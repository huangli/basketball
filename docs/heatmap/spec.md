# spec：进球热区图（v4.2：分区副图 + build 阶段集成）

日期：2026-08-16 · 前置：`docs/heatmap/research.md`、本文件 v1/v2/v3/v4/v4.1 修订史
提出：立哥 · 状态：v4.2（2026-08-16）——v4.1 已实施提交（0e8b40c）；
v4.2 两项：**副图蜂巢→分区统计替换**（立哥看图选定）+ **热图并入
video build 阶段自动生成**（立哥指令）；待 spec-reviewer 两轮审查

## 修订史

- v1：稳定段回溯法，Q2 实测 63.1% < 70% 证伪（死因：球在手上不停 +
  闲置球假命中）
- v2：locate_scorer 轨迹法，新 Q2 实测 55.7% < 90% 证伪（死因：球检测器
  看不到手上的球，"球→持球人"断链；56/68 真持球定位帧在锚点后）
- v3：roster 驱动追人 + 筐锚粗配准。覆盖 67/107 = 62.6% 过 55% 门槛，
  但**目击验收失败**：立哥判"框到的人不是我标记的进球人"——机检复核
  （落点帧框中人 `team_of_box` 队色 vs roster 队伍）**27/67 = 40% 队色
  相反必为错人**，另 18 球判便服待定；抽样可视化二次确认判色准确
- v4（本版）：根因 = 持球点搜索从轨迹末端回放，轨迹窗口含入网后 0.5s，
  球穿网下落穿过筐下球员躯干框——27 错球中 22 球种子时刻落在入网瞬间
  或之后（+0.3~+0.5s），追到的是防守人/捡球人；串人守卫只比种子帧与
  目标帧同色，两者错成同一人时放行。修复两条（见"v4 修复"节）。
  v4 重跑实测：覆盖 20/107 = 18.7%（守卫剔 12 错人；窗口剔 35 无种子球
  ——v2 老结论再现：球检测器看不到手上的球，持球点天然多在入网后）。
  阈值扫描（0.5/0.2/0.0/−0.3s 四档）证实放宽窗口多收的主要是错人球
  （机检一致仅 12→19），立哥定：保持 0.5s 口径（宁缺毋滥），
  覆盖率跌破 55% 由立哥裁决接受；目击验收暂缓（立哥：先不管人，下次继续）
- v4.1（2026-08-16）：渲染升级——立哥判旧渲染"看起来很 low"，4 风格
  打样（暗场霓虹/木地板/蜂巢/分区统计，work/heatmap_style_*.py 留档）
  后选定**暗场霓虹 + 蜂巢双风格**固化为正式渲染（见"v4.1 渲染"节）。
  已实施提交（0e8b40c）：实跑 3 球界外过滤，双风格各 2 张入库 output/
- v4.2（本版，2026-08-16）：① 立哥看打样分区图后定**副图蜂巢→分区统计
  替换**（蜂巢渲染器下线，上线不足一天无用户依赖）；② 立哥指令
  **热图并入 video build 阶段**自动生成（见"v4.2 分区副图与 build 集成"节）
- v1/v2 的 Q1/Q2 双门槛验证框架随 v2 证伪封存；release_probe.py /
  scorer_landings.py 留档（复算依据），不再发展

## v3 侦察依据（2026-08-15 实测，侦察时点 106 已标记球）

**分母口径（B1 写死）**：本节数字为侦察时点快照（106 球：车百鼎 53 /
半截篮 51 / 便服 2）；v3 实跑起并行会话又认 1 球，**实跑/成功标准分母
统一为 107**（车百鼎 53 / 半截篮 52 / 便服 2，roster confirmed=true）。

- hoops 筐轨迹覆盖 **121/122**（detected 且 window 含锚点 ±1s；例外 1 球
  `dji_mimo_20260805_190546_0035` 锚点 1.2s 落在其唯一 detected 事件
  window [1.5, 6.4] 之外）——粗配准锚点基本全有；
  锚点 cx 双峰（全量 detected 锚点 P25=563 / P75=1265，帧宽 1920），
  两端聚类平凡
- 进球轨迹选到 104/106；但轨迹完整（起点 ≥0.8s 前）仅 28/106 = **26%**
  ——"轨迹起点 ≈ 出手点"只在少数球成立，不能当主路
- 追人链（trace_person，定位框种子）活回 anchor−1.0s±0.3s：**63/106 = 59%**
- 串人/错人风险：56 球（占 68 个 v2 真持球）定位帧在锚点后（入网后人堆），
  种子框可能不是投篮者——抽样目击验收必须覆盖此项
- 队别解析实测：roster players 有独立 `team` 字段，assignments 值 = tag
  全量查得到（0 缺失）

## v4 修复（2026-08-15，写死）

针对 v3 目击证伪根因的两处修复，只改 `scripts/goal_heatmap.py`，
不动 crop_scorers 对外行为（认人链路已由立哥视频终裁，不受影响）：

1. **持球点搜索限出手前窗口**：轨迹先按 sec ≤ anchor − **0.5s**
   （新模块常量 `HELD_SEARCH_BEFORE_SEC`）截断——`Track(dets=[按 sec
   过滤])` 新建实例，不改原轨迹对象（兜底路仍用原轨迹起点）；
   `find_held_box` 与 `start_nearest_box` **均喂截断轨迹**——出手
   （anchor−1.0s 固定估计）前人持球，球心必在投篮者框内；入网后轨迹点
   不参与持球判定，筐下人不再能被链为种子。截断后无轨迹点 → **无种子，
   直接落兜底/no_landing**——晚起轨迹（起点 ∈ (anchor−0.5, anchor]）
   不再有"从侧门链回筐下人"的回退（S1 写死）。主路兜底其余约束不变
2. **队色硬守卫**：落点帧框中人 `team_of_box` 队色与该球 roster 队伍
   的期望队色**明确相反（黑↔白）时，该球计 uncovered**
   （reason=`team_mismatch`），主路兜底同此守卫；"便服"判定不剔除
   （无法机器判断，如实进报告）。期望队色映射按场次注入：
   `work/<场次>/session_facts.json` 新增 `team_color` 键
   （如 `{"车百鼎": "黑", "半截篮": "白"}`，对照 AGENTS.md 尺寸注入模式）。
   **启动校验（S2 写死）**：roster 实际队伍集合（**不含便服**——便服球在
   守卫前已剔除，team_color 不含便服）⊆ team_color 键集合，
   缺队伍 → 一次性 WARNING 且该队守卫禁用；键整体缺失 → 守卫全禁 +
   启动 WARNING（退化为 v3 行为，不静默）。
   不做"按队众数投票推队色"——半截篮实测错人率 53% 过半，投票会推反
3. **更正 v3 前提声明**：roster 已 `confirmed=true`（立哥页面终裁导出），
   v3 spec"confirmed=false 未确认风险"声明作废；v3 已证伪声明
   （"56 球定位帧在锚点后，种子框可能不是投篮者"）由本版闭环

## v4.1 渲染（2026-08-16，写死）

旧 matplotlib 简笔渲染被立哥判"很 low"下线，固化为**每队双风格两张 PNG**
（打样脚本 work/heatmap_style_dark.py / heatmap_style_hex.py 留档，
本节约口径即两打样的生产化）：

- **暗场霓虹（主图）**：沿用原文件名 `output/<场次>/队伍_XX_进球热图.png`
  ——近黑深蓝底 #0b1026、场地线双层发光（白粗低透明打底 + 白细核心）、
  连续高斯 KDE（σ=0.9m，0.05m 网格，不分箱）+ 归一后 gamma=1.2 压暗
  中低密度（稀疏点防整条雾带）、自定义全透明→青→黄→红 colormap、
  落点白点深描边置顶、筐圈霓虹橙点睛、标题+副标（场次 · n=X 球）、
  零刻度边框；视野 x∈[−8.3,8.3]、y∈[−2.6,12.9] 画满全场
- **蜂巢（副图）**：`output/<场次>/队伍_XX_进球热图_蜂巢.png`
  ——白底 + 全视野浅灰空蜂巢纹理、pointy-top 六边形（R=0.6m，自写网格，
  不用 ax.hexbin 防等比拉伸变形）、最近中心归格、截断 YlOrRd 着色、
  ≥2 球格中心标数字（按格色亮度自动选字色）、淡灰场地线压热力层之上、
  竖向 colorbar（进球数整数刻度）、报纸信息图版式；视野纵向收窄
  y∈[−2.5,7.9]（稀疏数据防上半场大片空白），横向 **x∈[−8,8] 固定**
  （生产版弃用打样的右缘动态外扩——界外过滤后界内余量仅 0.5m，
  外扩意义不抵构图一致性，S1 写死）。
  **视野外界内点口径（S2 写死）**：dy > 7.9 的界内落点（实测无，
  当前 y≤4.6）如出现 → WARNING 且**不入蜂巢图**（仍入暗场主图，
  暗场视野覆盖全场）——防 argmin 最近中心归格把点错位到视野缘
  篡改分布
- **界外点口径（两风格统一）**：渲染层过滤 |dx| > 7.5+0.5 或
  dy ∉ [−1.575−0.5, 12.425+0.5] 的落点（上游人框高尺度锚的已知噪声，
  实测 3 球 |dx| 最大 11.9m 物理不可能），逐球 WARNING + summary 记
  `out_of_bounds` 数；**goal_landings.json 原始数据不删**（留追查）；
  图上副标统一写 `n=X 球（界外 Y 球未入图）`（Y=0 省略括号段），
  防"热图 n 与集锦球数对不上"的对账困惑（J1）
- 渲染参数全部模块常量（σ/gamma/HEX_R/配色/视野），rules.md 禁魔法值

## v4.2 分区副图与 build 集成（2026-08-16，写死）

**副图替换（蜂巢 → 分区统计）**：`render_team_heatmap_hex` 及 HEX_* 常量、
hex_centers/bin_points 全部下线（蜂巢上线不足一天，无用户依赖），
由 `render_team_heatmap_zones` 接替，输出
`output/<场次>/队伍_XX_进球热图_分区.png`（替换 `_蜂巢.png`）。
分区口径（打样 work/heatmap_style_zones.py 的生产化，偏差写死）：

- **10 区几何照打样**：合理冲撞区（r=1.25m）/禁区其余/中距离左中右/
  底角左右/45°三分左右（分角 50°）/弧顶三分；`build_zones()`/（多边形 +
  标注位）与 `zone_of(x, y)`（归区）为纯函数可测；每区按占全队比例
  单色系填色（强度 = 0.25+0.75·√(占比/最大占比)，0 球区最浅 tint 保结构），
  区中心标"区名 / n球 / （占比%）"，底角区端线外标注+引线，落点小灰点叠加。
  **打样 bug 修正（写死）**：打样 zone_of 的 inside3 判定在 |x|>6.6 分支
  取值写反（应为恒 False 却写成 y≤corner_y）——底角条带被错判线内、
  corner 分支成死代码；生产版修正为
  `inside3 = (|x|≤6.6 且 r≤6.75)`（角条区恒在线外），底角三分正确归
  corner 区（对当前数据无影响：无角条落点）
- **界外点与暗场统一走 filter_in_court**——打样的"横向超界收拢到边线内
  归类显示"**弃用**（生产版不收拢不错位）；**余量带残余口径（S2 写死）**：
  filter_in_court 余量 0.5m 放行的点（|dx|∈(7.5,8.0] 或 dy<0）按 zone_of
  现行规则归最近语义区（计数守恒、散点位置如实；ra/paint 多边形只覆盖
  y≥0，余量带点可能归到多边形不含它的区——视为界外噪声归就近区，
  报告注明；当前实测 dy≈+1.9~+4.5 无实害）；
  副标统一 `_subtitle` 口径（场次 · n=X 球（界外 Y 球未入图）），
  打样的"对<对手>"副标弃用（build 阶段不知对手，K3 不写死）
- **队色系不写死队名（K3）**：固定调色板 [暖红系， 深蓝系， 深绿系， 紫系]
  按队名字典序分配（字典序 = Unicode 码位序，J3；可复现；>4 队取模循环；
  半截篮 U+534A < 车百鼎 U+8F66 → 暖红/深蓝，与打样图一致），
  标题/最深色同系
- 视野 x∈[−8.2,8.2]、y∈[−2.7,13.1] 画满半场（分区多边形覆盖全场，
  无蜂巢的视野外归格错位问题；filter_in_court 已把 dy 限在 ≤12.925）

**build 阶段集成（立哥指令）**：`video build` 非 dry-run 且合集合成
全部成功后，自动调 `goal_heatmap.heat_session` 出双风格图：

- 触发位置：`_cmd_build` 合集循环全部完成后、返回前；**在成功分支内
  懒 import goal_heatmap**（S4 更正：video.py 顶层无 matplotlib/numpy，
  goal_heatmap 顶层会拉入 crop_scorers 的 cv2/numpy/PIL——懒 import 防
  score/people/photo 白付导入成本）；dry-run 只 _log_dry_step 打印不执行
- **roster.json 缺失 → INFO 跳过**（J1：尚未认人是预期常态非异常，
  WARNING 留给 roster 存在但损坏等真异常；--all 路径 roster 必在，
  不受影响）
- **目录推导写死 goal_heatmap 侧（S3）**：`heat_session` 的
  detect_dir/frames_dir/out_dir 三参加 None 默认（None → session_dir
  同级 detect/、frames/、repo output/<场次>——推导逻辑从 main() 收进
  heat_session，video.py 不复制布局知识）；video.py 只传 session_dir
- **热图失败不阻塞 build**：heat_session 异常捕获后 log ERROR
  （含 run_id 与异常上下文）+ build 返回码不变（附属产物不阻塞主链；
  不静默——rules.md §0.2）；**build 收尾 log 一行热图结果**
  （出图/跳过/失败，J2/J5——"build 完成"与"热图没出"并存时可发现）
- **重算副作用写死（J2）**：每次触发全量重算并重写
  goal_landings.json / heatmap_audit.png / output PNG（同输入确定性、
  原子写，无数据风险）；耗时秒级~十秒级（逐 fid 读 mot_cache JSON +
  逐球 _team_at 读帧图 + 目击拼图读帧），build 变慢时不要误判合集卡住
- team_color 守卫沿用 session_facts 注入口径
  （键缺失 → 守卫禁用 WARNING，v4 已定）
- 文档联动（强制）：使用手册.html build 节补热图产物说明；
  `docs/video-cli/spec.md` §build 补热图步骤；AGENTS.md 统一入口 CLI
  行 "build=合集合成" → "build=合集合成+热图"

## 目标

基于 roster 已归属的 107 球，产出**球队级**进球热图（每队一张 PNG +
落点数据 JSON），用最小新代码回答"各队进球都分布在场上哪些区域"。
个人热图、精确米制统计均不在本期。

## 落点口径（v3 核心定义，写死）

**落点 = 投篮者人框底边中点（像素）→ 筐锚相对坐标。**两路并集，主路优先：

- **主路（追人链）**：种子 = 认人同逻辑定位框（find_held_box **限
  sec ≤ anchor−0.5s 窗口**（v4）/ start_nearest_box）→ `trace_person`
  IoU 链回追 → 取链上最接近 **anchor−1.0s** 的帧（容差 ±0.3s，5fps 即
  ±1.5 帧）→ 该帧人框底边中点。
  anchor−1.0s = 出手时刻固定估计（球飞行 0.5~1.5s 的中值；粗糙但稳定，
  接球就投/急停跳投场景出手前 1s 人已基本站定）
  - **结构性上限声明（S2）**：trace_person 窗口 = 种子帧前后各 2.0s 写死
    在 crop_scorers（不改对外行为）。种子帧早于 anchor−3.0s 时目标帧
    越出窗口，链再活也够不到——59% 实测已含此天花板，不靠扩窗提覆盖
- **兜底（完整轨迹）**：主路链断（±0.3s 内无链上帧）且进球轨迹起点
  ≥0.8s 在锚点前时，取轨迹起点帧的**最近人框**底边中点
  （球离手点最近的人必在出手点 1~2m 内，物理约束不依赖"谁"；
  最近人框可能是防守人——v4 起由队色硬守卫剔除明确相反者，
  残余"便服"误判噪声报告声明，O4）
- **队色硬守卫（v4）**：两路落点产出后统一过——落点帧框中人队色与
  该球 roster 队伍期望队色明确相反 → uncovered（`team_mismatch`）；
  期望队色从 session_facts.json `team_color` 键读入，缺失则守卫禁用 +
  WARNING（详见"v4 修复"节）。
  **两守卫分工（A3 写死，勿删其一）**：串人守卫保"链不漂"（链内一致性，
  种子帧 vs 目标帧，触发→视同链断走兜底）；队色硬守卫保"人对"
  （身份正确性，落点人 vs roster 期望，触发→uncovered 不重试）——
  判定基准不同，前者给主路一次兜底机会，后者是终闸
- 两路都无 → 记 uncovered，如实进报告（不硬凑）
- **串人守卫（S1 更正接口）**：链上目标帧人框与种子框做颜色队别校验——
  用 `team_of_box`（整帧图 + 人框，crop_scorers:1118），两者分类明确相反
  （黑↔白）时主路不可用、**视同链断走兜底**（plan 审查写死：两路并集的
  精神是主路不行走兜底，兜底为独立几何约束不受链污染）；"便服"判定
  不触发剔除（与认人 drop_opposite_team 同口径）。
  **帧图因此进入主路径依赖**（不只目击拼图用）
- 广告静物风险（S6）：同场次已实测 IoU 链会漂到场边广告"人形"静物
  （经验教训 §3），颜色守卫防不住（广告归便服不剔除）——列入目击判据
  （"框是不是真人"）与报告风险项

## 坐标口径（粗配准，写死）

**不做单应、不做人工点位**（Q1 路线封存）。逐球相对坐标：

- 原点 = 该球锚点时刻的**筐心像素**（hoops 事件 track 按采样时刻最近取值；
  多事件 window 同时覆盖锚点时，取采样时刻距锚点最近者，仍并列取锚点
  空间距离最近者——S4 写死取舍；**零覆盖时**（无 window 含锚点，实测 1 球）
  取采样时刻距锚点最近的 detected 事件并记 WARNING——J1 写死分支）
- **筐端归一化（B1，全场双筐必须做）**：hoops 锚点 cx 聚成两端（按 cx
  中位切分；本场双峰实测 P25=560/P75=1265）；统一归一到 **cx 均值较小端**
  ——较大端事件的落点**只水平镜像 dx，不翻 dy**（实测定版：场边机位下
  画面纵深方向对两端筐一致，人总在筐的场内一侧；全量旋转 180° 会把大端
  33 球 dy 全镜像到端线外 [−4.3,−1.9]，只翻 dx 后两组 dy 分布一致
  [+1.9,+4.5]）。翻转标记（flipped: bool）逐球落盘并进报告
- 尺度 = 该落点帧**人框高** ÷ 假设身高 1.75m（像素→米，逐球局部尺度；
  人框截断/遮挡会带噪声，接受并在报告声明；人框贴边时底边中点与框高
  双失真，落点噪声一并声明——O5）
- 方向：不校正旋转——同场次同机位（场边固定），画面水平轴 ≈ 场地
  边线方向，镜头变焦/平移由筐心+尺度吸收
- 输出 (dx, dy) 米制相对坐标；渲染时映射到 matplotlib 标准半场模板
  （筐心对齐模板筐心）

## 队别口径（B2 写死）

- team = roster assignments 值（tag）→ players 按 tag 查 `team` 字段
- 本场队伍集合动态取 players 的 team 去重（实测：车百鼎/半截篮/便服）；
  team 为"便服"的归属（实测 2 球）**不进热图**，逐球 WARNING 并在报告
  注明数量；队伍集合变化时热图按实际集合出图（不写死队名——K3）。
  **便服球在队色硬守卫之前已剔除，`team_color` 映射无需也不含便服（A6）**
- tag 查不到 team → WARNING 记 uncovered（数据损坏苗头，不静默）
- 队名渲染用 roster 原值（"车百鼎" / "半截篮"）

## 交付物

1. `scripts/goal_heatmap.py` + 单测（纯只读现有产物）：
   - 输入：roster.json + glob 发现的 `goals_batch*.json` /
     `candidates_batch*.json` / `hoops_batch*.json`（不硬编码批次号，O2）+
     work/detect/<fid>_mot_cache.json + work/frames/<fid>/f_*.jpg
     （串人守卫 + 目击拼图用）；全部 schema 校验先行；缺缓存记 WARNING
     计 uncovered
   - 逐球：落点两路并集 → 筐端归一化相对坐标 → 输出
     `work/20260805_车百鼎/goal_landings.json`（每球：event_key/team/
     路径（trace|track_start）/landing_px/rel_xy_m/flipped/覆盖状态）
   - 渲染（v4.2 双风格）：暗场霓虹主图 `队伍_XX_进球热图.png` +
     分区统计副图 `队伍_XX_进球热图_分区.png`，每队两张落 `output/<场次>/`
     （口径见"v4.1 渲染"与"v4.2"节；界外点渲染层过滤 + WARNING +
     summary 计数）；**通常不由本脚本单独触发——`video build` 阶段自动
     生成（v4.2 集成），本脚本 CLI 保留作单独重跑入口**
   - 抽样目击：随机（固定种子）抽 15 个覆盖球，生成目击拼图
     `work/20260805_车百鼎/heatmap_audit.png`——每球并列：**落点帧**
     （叠加人框矩形 + 落点十字）+ **锚点帧缩略**（供比对投篮者，O3）+
     文字标注（event_key / rel_xy_m / flipped / 映射后模板落点）
2. 验收报告：`docs/heatmap/calib-report.md`（新建）——覆盖率实测、
   两端锚点分布与翻转端标记、抽样目击结论（立哥判）、坐标口径误差声明
   （人框高尺度 + 无旋转校正的米级误差 + 贴边失真）、**覆盖选择性偏差
   声明（主路偏好静止出手，突破/强对抗易断链——S5 强制项）**、
   **队色硬守卫剔除数（team_mismatch）与"便服"判定的残余错人噪声
   声明（v4 强制项）**、广告静物链风险
   （S6，框可能不是真人）、便服球剔除数量、
   明确"热图是否可用作展示"结论

## 边界（不做）

- 不做个人热图（球队版验收通过后再议，另起 spec）
- 不做米制单应配准、不做人工点关键点（v1/v2 的 Q1 路线封存）
- 不追求 107 球全覆盖（热图是密度可视化；未标记的 15 球不管——立哥原话；
  覆盖的选择性偏差在报告中声明，不宣称"不歪分布"）
- 不改 `scripts/` 下 `goal_heatmap.py` 以外任何文件的对外行为
  （crop_scorers 的 find_held_box 等保持原样，v4 窗口截断在
  goal_heatmap 内做）；**v4.2 例外（S1 写死）**：video.py 仅在
  `_cmd_build` 成功返回前追加热图触发（懒 import + 异常不阻塞），
  不改其既有命令拼装/过滤/返回码语义；不改 roster/goals 等任何产物
  （session_facts.json 追加 `team_color` 键除外——按场次注入模式）
- 不做视频叠加/逐球回放校验页（一张拼图搞定目击）

## 成功标准（预先写死，防现场改目标）

- **框人正确性（v4 首门）**：重跑后机检复核——covered 球中落点帧框中人
  队色与 roster 队伍明确相反者必须为 **0**（守卫结构性保证，独立脚本
  复核验证守卫真的生效而非空转）；"便服"判定数如实记录
- **覆盖率**：107 已标记球（含便服 2 球作分母——K1 写死；便服球天然
  不覆盖）中两路并集落点覆盖 **≥55%**；若因队色硬守卫剔除跌破 55%，
  报告如实记录并由立哥裁决是否够用（宁缺毋滥，不硬凑——v3 教训：
  覆盖率过关≠落点可信）
- **抽样目击**：15 球拼图立哥肉眼判 **≥10 球**通过——判据（O3 量化）：
  ① 人框是真人且是投篮者本人或紧贴出手点（对照锚点帧）；
  ② 映射后落点在半场界内且距筐心 ≤10m
  ——不过则记档收敛（热力图路线整体证伪，四次出手到此为止）
- **产出**：每队双风格 PNG（暗场主图 + 分区副图）+ goal_landings.json +
  calib-report.md
- 质量门：ruff format/check 干净、pytest 全绿（v4 新增单测：
  持球点窗口截断用例（入网后点不参与）、队色硬守卫剔除/便服放行/
  守卫禁用退化用例；v4.1 新增：暗场渲染 smoke（空点集/有点集出图）、
  界外过滤已知输入；v4.2 新增：zone_of 归区已知输入、build_zones
  几何合法性、分区渲染 smoke、video build 集成——热图自动触发/
  失败不阻塞/roster 缺失跳过/dry-run 不执行）

## 依赖与环境

- 全部复用：crop_scorers（locate 链路/run_mot/trace_person/team_of_box）、
  hoops/candidates/goals/roster 现有产物、matplotlib（已装）
- 帧图用途：串人守卫颜色判定（team_of_box）+ 目击拼图
  （work/frames/<fid>/f_*.jpg 现取）
- 队色映射：`work/<场次>/session_facts.json` 的 `team_color` 键（v4 新增，
  按场次人工注入；run_session.py 自动写入该键属后续跟进项，见待办）

## 开放问题

1. 身高假设 1.75m 写为模块常量可调；若目击发现尺度系统性偏差，
   报告中注明而不现场调参（调参属下一阶段）
2. 便服队员进球队热图不计——若立哥想要"含便服的全员热图"，后续另议
