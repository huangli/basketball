# review02：阶段 0 spec 复审存档（spec-reviewer，2026-08-15）

## 结论：通过（无阻断、无冲突），可进入 plan/todo 阶段

## 首轮阻断修复核验

- **B1 ✓**：≥5 点、沿纵深铺开、留一法 n−1≥4 数学约束写清；Q1 改留一法逐点评估
- **B2 ✓**：出手点口径独立成节（落点=持球人 persons 框底边中点，球位定时刻/定人）；
  代码侧核实 mot_cache persons 为逐帧 [x1,y1,x2,y2] 裸框、底边中点可直接取，
  crop_scorers 轨迹法（find_held_box + trace_person + load_mot_cache 校验）可复用
- S1~S7、O1~O3 全部落地，无新引入阻断

## plan 阶段收口项（复审建议，不阻断）

- P1 多稳定段取段规则：出手时刻 = 窗口内**最后一段稳定段的末帧**（球离手即出手），
  人框取该帧持球人框——plan.md 第一句写死
- P2 稳定段判据实现时收紧为"段内相邻有球帧位移均 ≤60px"（成本相同更严）
- P3 报告加"人框贴帧边/与相邻人框重叠"观察项（不改判据，只加可信度分层）
- P4 calib.json 的 event_key 规则实现时定（建议 fid@anchor_time）
