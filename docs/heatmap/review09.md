# review09：v2 证伪收尾文档审查（2026-08-15）

审查对象：`docs/经验教训.md` §8 新增三条、`docs/heatmap/todo.md` v2 状态收尾、
`docs/heatmap/spec.md` 头部状态行
审查员：spec-reviewer 子代理 · 结论：**通过（无阻断）**，建议改进 1 条 + 可选优化 4 条

## 整体评价

三处改动范围与声明一致，所有实测数字均可用 scorer_landings.json 与源码复核通过，
口径与 spec v2 成功标准、todo 状态、v1 证伪条目之间无矛盾。

## 数字复核记录（全部通过）

- 总数 122；真持球 68 = 55.74%（q2_pass=false）✓；start_fallback 52 ✓；
  SKIP 2 均 no_track_near_anchor ✓（no_track/no_person 为 0，"断链在球→持球人"
  归因成立）
- 时间分布：68 真持球中锚点后 56 ✓；合理出手窗（≥0.3s 前）仅 10 ✓
- 目击条目：...@10.7 frame_idx 55 ↔ f_00056.jpg、sec=11.0、box=[1095,472,1211,822] ✓
- todo「11 新单测」：tests/test_scorer_landings.py 实测 11 个 test 函数 ✓
- 统计陷阱：_process_goal 仅 SKIP 分支覆写 reason（crop_scorers.py:1354-1391），
  OK+start_fallback 不落盘——陷阱描述属实 ✓

## 建议改进（已采纳落实）

1. 抽帧目击球衣号码 #24 → 实为 #9（裁切框区核实；实质论点不受影响）。已改，
   并泛化表述为"篮下深色队 #9"。

## 可选优化（采纳 2/4）

1. v1"若未来重启热图"条末尾加交叉指针（该筐锚定路线 v2 已证伪）——已加。
2. "全程无一帧"措辞稍泛（指 locate 窗口内所选轨迹整轨）——不改为可接受。
3. spec.md 修订记录 97.4% 先验未加注陷阱——历史修订依据保留原文，不改。
4. "仅 10 球（8.2%）"补分母（122）——已补。

## 与 AGENTS.md 冲突对照表

无冲突。证伪记档、未启动 Task 标注、建议 vs 计划边界、"每条须有实测依据与出处"
均合规。
