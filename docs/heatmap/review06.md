# review06：spec.md v2 第 2 轮复审（2026-08-15）

审查对象：`docs/heatmap/spec.md`（v2，第 1 轮 6 处修订落实后）
审查员：spec-reviewer 子代理 · 结论：**通过（无阻断、无建议改进）**，可选优化 3 条

## 整体评价

第 1 轮的 3 条建议改进 + 3 条可选优化全部落实且落实正确，未改出新的矛盾。
全文重扫确认数值、口径、schema 字段名前后一致，交付物 3 无 `frame` 旧名遗留。
spec v2 已达交付立哥批准的状态。

## 6 处修订落实核对（全部 ✅）

1. 抽样口径（交付物 2）："可用落点球（OK 且非 start_fallback）"——与口径节
   "start_fallback 不计入可用落点"对齐，缝隙消除。
2. SKIP 落盘形态（交付物 1）："frame_idx=-1、person_box/landing_px=null，
   字段不省略"——与 LocateResult 实际行为一致，消费方说明清楚。
3. event_key 自包含定义（交付物 1）：P4 降为出处注解，悬空问题解决。
4. calib.json `frame` → `frame_idx`；交付物 3（calib_eval）重扫无旧名遗留。
5. 阈值依据（~7pt 余量 / batch3 无先验）写入修订记录，与成功标准数值无冲突。
6. 点位缓冲为建议性质，≥5 判据本身未动。

## 可选优化（不阻塞；已顺手吸收 2/3，见下）

1. "折内仍有 5 点缓冲"措辞：6 点剔除 1 点剩 5 点，留一折内拟合用 n−1=4 点
   恰为下限。→ 已改为"仍剩 5 点、折内 4 点满足留一法下限"。
2. SKIP 时 `sec` 字段形态未写死 → 已补 sec=-1.0。
3. 交付物 1 schema 行与紧随其后的 event_key 自包含定义轻微冗余（同段说两次）
   → 已删 schema 行内的"沿用 P4"尾巴。

## 全文重扫记录（无新增矛盾）

- 数值：122 = 29+49+44、≥110 球、97.4% 先验、窗口 [anchor−4.0, +0.5]、
  留一 n−1 ≥ 4 / 折内 4~7 点——前后一致。
- 口径：start_fallback 计未命中、缺缓存计分母、SKIP 不进热图事件源——
  三处呼应一致。
- schema：landings 字段集与 SKIP 写死形态、calib.json 字段集无交叉冲突。

## 与 AGENTS.md 冲突对照表

无冲突。四件套位置、work/ 中间产物落盘、rules.md §0.2 输入校验、只读复用
不改对外行为、reviewNN 与 calib-report 命名隔离等约定维持合规。
