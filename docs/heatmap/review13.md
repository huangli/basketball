# review13：plan/todo v3 第 2 轮复审（2026-08-15）

审查对象：`docs/heatmap/plan.md`、`docs/heatmap/todo.md`（v3，第 1 轮 6 处
修订落实后）；spec.md 串人守卫条同步修订一并核对
审查员：spec-reviewer 子代理 · 结论：**通过（无阻断、无建议改进）**，可选优化 2 条

## 整体评价

第 1 轮 3 条建议改进 + 3 条可选优化全部如实落实，无走样、无新增矛盾。
"守卫剔除走兜底"在 spec 与 plan 两处口径逐字一致，与 ≥55% 覆盖率余量
逻辑自洽；新增代码级表述经源码核对均准确。

## 6 处落实逐条核对（全部 ✓）

- 建议 1：plan 步骤 3 与 spec 守卫条均写死"黑↔白明确相反时主路不可用、
  视同链断走兜底"；余量分析：剔除后兜底接不住才损失覆盖，55% 门槛更保守，
  方向安全
- 建议 2：todo Task 3"分母 106，含便服 2 球" ✓
- 建议 3：plan 步骤 5"1.75m 为模块常量可调" ✓
- 可选 1：todo Task 1 补 event_key ✓
- 可选 2：match_anchor_xy 超差 → None → 端点时间最近（认人同口径）；
  select_goal_track None 仅"无轨迹/端点超 200px"两支 → uncovered——
  源码逐支核对准确（crop_scorers.py:750/790/806-813）
- 可选 3：帧文件名 f_{frame_idx+1:05d}.jpg 与 crop_scorers.py:1207-1208
  逐字一致

## 可选优化（已顺手吸收）

1. 单测用例名跟上守卫新语义："串人守卫剔除→走兜底（兜底断才 uncovered）"
   ——plan 单测清单已改。
2. spec 覆盖率余量措辞补"（剔除后兜底接不住才损失）"——已补。

## 与 AGENTS.md 冲突对照表

无冲突（约定合规状态同第 1 轮）。
