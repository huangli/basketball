# review01: photo-select 第一轮实现与实测

日期：2026-08-15 审查人：主会话（实现经 coder 子代理，中断后由主会话收口）

## 实现摘要

- `scripts/rank_photos.py`（865 行）：mot_cache 打分（球速 0.35/人球交互 0.25/主体尺度 0.25/球高度 0.15，
  球 conf≥0.35 取 max）→ 10s 分桶每桶保底 1 张再按分补齐 → ffmpeg `-ss` 前置精确 seek 抽 ±3 原帧共 7 帧，
  Laplacian 方差选清晰（下限 15，夜场实测标定）→ 构图裁切（最大人框主体、球联合包围、比例跟随素材、
  头顶留白、三分法、边界夹取）→ JPEG q95 + photo_candidates.json（原子写）+ `--apply` 落盘照片精选
- `scripts/gen_photo_page.py`：自包含瀑布流确认页（点选/快捷键/导出 selections JSON）
- `scripts/video.py` 加 `photo` 子命令；`使用手册.html`、`AGENTS.md` 已同步
- 测试：`tests/test_rank_photos.py`、`tests/test_gen_photo_page.py`（打分/分桶/构图/尺度断言/页面）

## 关口

- `ruff format` / `ruff check --fix`：通过（子代理残留文件 `scripts/_demo_three_step.py` 挡关口，已删）
- `pytest -q`：146 全绿

## 实测（20260805_车百鼎，148 个 4K HEVC 59.94fps 短片）

- 抽帧+裁切耗时约 25 分钟（含中断后续跑；断点续跑验证有效，已抽帧不重复）
- 产出 211 张候选（221 个候选点，10 组全帧模糊整组丢弃，记 WARNING）
- 时间覆盖：全场 36.6 分钟，最差 10 分钟窗口 53 张（spec 要求 ≥5，远超）
- 视频覆盖：121/148（27 个无人或无可构图帧，符合预期——多为远镜/空场片段）
- 清晰度：min 16 / 中位 46；放大惩罚 0 张（4K 底全部无损裁切）
- 页面：`work/20260805_车百鼎/photos/photo_page.html` 生成成功

## 构图抽查（10 张：c001/c025/c051/c076/c102/c127/c157/c183/c210/c221）

- 合格 8/10：主体突出、不切头、球在画面内、动作瞬间抓得准（传球/突破/篮板/封盖均有）
- c051/c076 有可见运动模糊：夜场快门下限所致，Laplacian 阈值 15 偏宽容（min 16 即此类边缘帧）
- 无一张定位错主体（最大人框策略在本场镜头距离下可靠）

## 遗留问题（不阻断交付）

1. 清晰度下限 15 偏松，边缘帧混入率约 10~20%——人工瀑布流终裁可兜住；若要收紧建议提到 25 再实测（改一行常量）
2. 4:3 老场次链路已实现（比例跟随 + 1920×1440 坐标系断言）但无实测场次验证
3. 确认页 → selections.json → `--apply` 落盘链路已实现未实跑（等立哥页面点选后验证）
