# Review 01: event-split spec/plan/todo 审查（2026-09-06）

> 审查员：spec-reviewer 角色子代理（只读审查）。结论：**通过**。

## 核对结果

- 根因与设计一致（行号/常量全部实证吻合）；设计达成用户口径且不触碰补篮实证约束（6s/400px 不动）。
- 800px/20s 初值风险被"临时目录验证 + 立哥过目后才动正式产物"兜住；O1 显式挂出。
- 验证方案零风险；成功标准可测。

## 非阻断提示（实施留意）

- "缺筐退球位"锁定为**逐对退回**（一对中任一缺筐该对用球位比较）。
- 单测显式补一条：同场地镜头大幅移动致距离 >800px 的场景（防误切用例）。
- 经验教训.md 与代码注释中同球对 anchor 差口径不一致（4.2s vs 2.6s），T1 摸底顺带核清。
