# review06：triage 改悬停视频墙（立哥第二次验收反馈）

日期：2026-08-11　审查方式：立哥拍板方向 + 主会话实施 + 实测
判定：**通过（待立哥实操验收新版墙）**

## 起因（验收反馈）

筐区裁剪缩略图（review05）交付后立哥再次实操反馈：**静态图片人类
无法判断是否进球**——"进网还是弹筐而出"是运动过程判断，单帧/三帧
静态图先天不足。立哥在"悬停视频墙 / 筐区裁剪视频墙 / 放弃扫尾墙"
三选项中拍板**悬停播放视频墙**。

## 修复内容

1. **卡片主区域 = 悬停播放视频**：复用 events_index.json 已有的 clip
   字段（gen_review_clips --keep-clips 产物，840×840 筐区方形特写、
   2x 慢放已烘焙，
   零新计算）；`preload="none"` + poster=锚点帧裁剪缩略图（未悬停时
   仍是缩略图墙，加载快）；mouseenter 播放 / mouseleave 停并回开头，
   循环静音。
2. **attach_clips()**：挂接前检查 clip 文件存在性，缺失/无字段事件
   clip 置 None 降级纯静态卡片 + 记降级清单；Windows 反斜杠路径
   规范化为正斜杠。
3. 缩略图（筐区裁剪）保留为 poster 与 ±0.4s 小图参考，review05 的
   裁剪/锚点逻辑不动；三条交互硬规定未动。

## 验证证据

- 批次 3 全量重生成：234/234 事件 clip 全部挂上（0 降级）、
  锚点 234/234；页面 HTML 含 234 处 clips/ 引用
- 页面 `<script>` 段 node --check 通过
- pytest 470 passed（新增 4 用例：attach_clips 挂接/降级、视频卡
  断言、静态回退断言）、ruff format+check 全绿

## 口径澄清

- 选 clip（840×840 筐区方形特写、2x 慢放）而非 clip_wide（840×472 全景、
  2x）：特写全程见筐、入网瞬间拉长更易判；立哥此前已用同款 2x 审核片段
  判完 51 球，判读可行性已实证
- 悬停播放依赖浏览器本地 file:// 加载 mp4（与 label.html 同形态，
  已验证可用）
- spec.md F1 已同步视频墙口径
